import { Text } from "react-native";

const Jobs = () => {
  return (
    <Text
      style={{
        fontSize: 24,
        fontWeight: "bold",
        alignSelf: "center",
        marginTop: 100,
      }}
    >
      Jobs Screen !
    </Text>
  );
};

export default Jobs;
